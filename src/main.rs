use tokio::time;

async fn run() {
    log::info!("Sleeping!");
    time::sleep(time::Duration::from_secs(1)).await;
    log::info!("Awake!");
}

fn main() {
    simple_logger::init_wi
    println!("Hello, world!");
}
